# ─────────────────────────────────────────────────────────────────────────────
#  Раши 2.1 — requirements.txt
#  Python 3.12+
# ─────────────────────────────────────────────────────────────────────────────

# ── Telegram Bot ─────────────────────────────────────────────────────────────
# PTB v21+ обязателен: поддержка Telegram Stars (currency="XTR"),
# ApplicationBuilder.post_init/post_shutdown, concurrent_updates
python-telegram-bot==21.9

# ── AI / Claude ───────────────────────────────────────────────────────────────
# AsyncAnthropic + исключения APIError, APITimeoutError, RateLimitError
anthropic>=0.40.0

# ── База данных ───────────────────────────────────────────────────────────────
# Async SQLAlchemy ORM (engine, sessionmaker, DeclarativeBase)
sqlalchemy>=2.0.36

# Async-драйвер для SQLite (локально / Railway)
aiosqlite>=0.20.0

# Async-драйвер для PostgreSQL (Railway Postgres addon — раскомментировать при переходе)
# asyncpg>=0.29.0

# ── Планировщик ───────────────────────────────────────────────────────────────
# AsyncIOScheduler: interval + cron триггеры
# Используем ветку 3.x (4.0 ещё в бете)
apscheduler>=3.10.4,<4.0

# ── Геокодинг и таймзоны ──────────────────────────────────────────────────────
# Nominatim: город → (lat, lon, название)
geopy>=2.4.1

# lat/lon → название таймзоны (Europe/Moscow и т.д.)
timezonefinder>=6.5.3

# Конвертация UTC ↔ локальное время в планировщике
pytz>=2024.2

# ── Ведическая астрология ─────────────────────────────────────────────────────
# VedAstro Python-обёртка (Calculate, GeoLocation, Time)
# Импорт защищён try/except в astrology.py: бот запустится без неё,
# но расчёт карт будет недоступен.
# Уточни актуальную версию на pypi.org/project/vedastro/
vedastro>=0.0.6

# ── Переменные окружения ──────────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── HTTP (транзитивная зависимость PTB, явно для предсказуемости) ─────────────
httpx>=0.27.0
